import { GeoPackage } from '@ngageoint/geopackage';

/**
 * Handle File Load / Change, Send to Controller for Conversion
 * 
 * @param file to be converted and loaded
 * 
 * @returns {GeoPackage} file
 */ 

export default function Converters(file: File | null): GeoPackage {
    /**
     * TODO: Convert file to GeoPackage. 
     * Determines what type of file it is and calls the corresponding method.
    */
   throw new Error("Not Implemented");
};